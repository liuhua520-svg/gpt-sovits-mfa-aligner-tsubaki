--extra-index-url https://download.pytorch.org/whl/cu121

# ====================== 核心依赖 & 严格版本锁定 ======================
numpy==1.26.4               # 【关键修复】强制锁定 1.x，解决 NumPy 2.x 导致的 C-API 崩溃
setuptools<82.0.0           # 解决 ctranslate2 / whisperx 对 pkg_resources 的依赖报错

# ====================== PyTorch (默认 CUDA 12.1) ======================
# 如果你没有独立显卡或只想用 CPU，请将后缀 +cu121 改为 +cpu，并删除顶部的 --extra-index-url
torch==2.3.1+cu121
torchaudio==2.3.1+cu121

# ====================== 核心框架 & 网络 ======================
flask==2.3.3
flask-cors==4.0.0
requests
tqdm

# ====================== 语音处理基础 ======================
soundfile==0.12.1
librosa>=0.10.0
resampy>=0.4.2
pyworld>=0.3.4
torchcrepe==0.0.24
kalpy

# ====================== MFA 与文本处理 ======================
montreal-forced-aligner==3.3.9
textgrid==1.5
pypinyin==0.53.0
pycantonese>=0.1.0
sudachipy==0.6.8
sudachidict-core==20240409
jamo==0.4.1
nltk>=3.8.1
num2words>=0.5.13
g2p_en>=0.3.1

# ====================== 替代对齐后端 ======================
whisperx>=3.2.0
ctranslate2>=4.3.0
funasr>=1.1.0
modelscope>=1.9.0
accelerate>=0.27.0
qwen-asr>=1.0.0
k2

# ====================== 工程文件导出 ======================
ruamel.yaml>=0.18.0
mido>=1.3.0
