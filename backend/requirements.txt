flask==2.3.3
flask-cors==4.0.0
montreal-forced-aligner==3.3.9
pypinyin==0.53.0
textgrid==1.5
pycantonese==3.3.0
sudachipy==0.6.8
sudachidict-core==20240409
jamo==0.4.1
soundfile==0.12.1
numpy==1.24.3
pyworld==0.3.4

# ---- F0 提取扩展：CREPE / RMVPE（可选，缺失时仅 dio/harvest 可用）----
torch>=2.0.0
torchcrepe==0.0.24
librosa>=0.10.0

# RMVPE 模型权重 (rmvpe.pt, ~180MB) 不随依赖分发，需手动下载并放置到
# <项目根>/models/rmvpe/rmvpe.pt，或设置环境变量 RMVPE_MODEL_PATH 指向该文件：
#   https://huggingface.co/lj1995/VoiceConversionWebUI/resolve/main/rmvpe.pt
# ---- MIDI 文件导入（可选，缺失时 MIDI 功能不可用）----
mido>=1.3.0
